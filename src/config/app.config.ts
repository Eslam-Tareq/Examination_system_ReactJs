/**
 * App config – global switch for mock vs real backend APIs.
 * Set USE_MOCK = false when backend is ready to use real API calls.
 * All services (auth, exams, questions, courses) respect this flag.
 */
export const USE_MOCK = true;
